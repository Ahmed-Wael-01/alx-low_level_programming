#include "main.h"

/**
*  _abs - print alpha
* @n: the number to compare
*
* Description: it does smthing
* Return: 0
*/
int _abs(int n)
{
	int remain;
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (- n);
	}
}
