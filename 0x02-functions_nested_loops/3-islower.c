#include "main.h"

/**
*  _islower - print alpha
* c - the number to compare
*
* Description: it does smthing
* Return: 0
*/
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
