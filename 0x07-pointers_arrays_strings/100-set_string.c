#include "main.h"
#include <stdio.h>

/**
 * set_string - does something
 * @s: a variable
 * @to: a variable
 *
 * Description: a function
*/

void set_string(char **s, char *to)
{
	**(s) = *(to);
}
