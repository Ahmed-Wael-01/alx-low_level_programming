#include "main.h"

/**
*  mul - it does something
* @a: a variable
* @b: a variable
*
* Description: function that checks for uppercase character
* Return: 0
*/

int mul(int a, int b)
{
	return (a * b);
}
